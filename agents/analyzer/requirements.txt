# Log Analyzer Agent Requirements
# Inherits from shared requirements

-r ../shared/requirements.txt

# Additional analyzer-specific dependencies (if needed)
# None required - using built-in JSON parsing
